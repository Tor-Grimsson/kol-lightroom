/**
 * App identity — name/slug for page titles and asset-path fragments.
 * App-local config; the brand color layer lives in src/styles/kol-brand-color.css.
 */

export const APP = {
  name: 'kol-lightroom',
  nameSlug: 'kol-lightroom',
}
